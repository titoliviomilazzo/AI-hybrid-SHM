# AI-SSBMD web platform (app_shm.py) + batch CLI (batch_run_analysis.py)
streamlit>=1.28
torch>=1.9
numpy>=1.21
scipy>=1.9
pandas>=1.4
matplotlib>=3.6
seaborn>=0.12
scikit-learn>=1.0
tqdm>=4.60

# Note: app_shm.py uses tkinter for native file dialogs (local desktop use).
# tkinter ships with standard CPython on Windows/macOS.
# On Debian/Ubuntu Linux, install it separately:  sudo apt install python3-tk
